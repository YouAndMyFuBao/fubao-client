import { css } from '@emotion/react';

export const base = css({
  display: 'flex',
  justifyContent: 'flex-end',
  alignItems: 'center',
  padding: '10px 8px',
  height: '39px',
  background: 'red',
  zIndex: 0,
});

export const hamburger = css({
  cursor: 'pointer',
});
